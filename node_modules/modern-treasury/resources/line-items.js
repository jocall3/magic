"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
Object.defineProperty(exports, "__esModule", { value: true });
exports.LineItems = void 0;
const resource_1 = require("../core/resource.js");
const pagination_1 = require("../core/pagination.js");
const path_1 = require("../internal/utils/path.js");
class LineItems extends resource_1.APIResource {
    /**
     * Get a single line item
     *
     * @example
     * ```ts
     * const lineItem = await client.lineItems.retrieve('id', {
     *   itemizable_type: 'expected_payments',
     *   itemizable_id: 'itemizable_id',
     * });
     * ```
     */
    retrieve(id, params, options) {
        const { itemizable_type, itemizable_id } = params;
        return this._client.get((0, path_1.path) `/api/${itemizable_type}/${itemizable_id}/line_items/${id}`, options);
    }
    /**
     * update line item
     *
     * @example
     * ```ts
     * const lineItem = await client.lineItems.update('id', {
     *   itemizable_type: 'expected_payments',
     *   itemizable_id: 'itemizable_id',
     * });
     * ```
     */
    update(id, params, options) {
        const { itemizable_type, itemizable_id, ...body } = params;
        return this._client.patch((0, path_1.path) `/api/${itemizable_type}/${itemizable_id}/line_items/${id}`, {
            body,
            ...options,
        });
    }
    /**
     * Get a list of line items
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const lineItem of client.lineItems.list(
     *   'itemizable_id',
     *   { itemizable_type: 'expected_payments' },
     * )) {
     *   // ...
     * }
     * ```
     */
    list(itemizableID, params, options) {
        const { itemizable_type, ...query } = params;
        return this._client.getAPIList((0, path_1.path) `/api/${itemizable_type}/${itemizableID}/line_items`, (pagination_1.Page), {
            query,
            ...options,
        });
    }
}
exports.LineItems = LineItems;
//# sourceMappingURL=line-items.js.map
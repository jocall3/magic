import { APIResource } from "../core/resource.js";
import { APIPromise } from "../core/api-promise.js";
import { Page, type PageParams, PagePromise } from "../core/pagination.js";
import { type Uploadable } from "../core/uploads.js";
import { RequestOptions } from "../internal/request-options.js";
export declare class Documents extends APIResource {
    /**
     * Create a document.
     */
    create(body: DocumentCreateParams, options?: RequestOptions): APIPromise<Document>;
    /**
     * Get an existing document.
     */
    retrieve(id: string, options?: RequestOptions): APIPromise<Document>;
    /**
     * Get a list of documents.
     */
    list(query?: DocumentListParams | null | undefined, options?: RequestOptions): PagePromise<DocumentsPage, Document>;
}
export type DocumentsPage = Page<Document>;
export interface Document {
    id: string;
    created_at: string;
    discarded_at: string | null;
    document_details: Array<Document.DocumentDetail>;
    /**
     * A category given to the document, can be `null`.
     */
    document_type: string | null;
    /**
     * The unique identifier for the associated object.
     */
    documentable_id: string;
    /**
     * The type of the associated object. Currently can be one of `payment_order`,
     * `transaction`, `expected_payment`, `counterparty`, `organization`, `case`,
     * `internal_account`, `decision`, or `external_account`.
     */
    documentable_type: 'counterparty' | 'expected_payment' | 'external_account' | 'identification' | 'incoming_payment_detail' | 'internal_account' | 'organization' | 'payment_order' | 'transaction' | 'connection';
    file: Document.File;
    /**
     * This field will be true if this object exists in the live environment or false
     * if it exists in the test environment.
     */
    live_mode: boolean;
    object: string;
    /**
     * The source of the document. Can be `vendor`, `customer`, or `modern_treasury`.
     */
    source: string;
    updated_at: string;
}
export declare namespace Document {
    interface DocumentDetail {
        id: string;
        created_at: string;
        discarded_at: string | null;
        document_identifier: string;
        document_identifier_type: string;
        /**
         * This field will be true if this object exists in the live environment or false
         * if it exists in the test environment.
         */
        live_mode: boolean;
        object: string;
        updated_at: string;
    }
    interface File {
        /**
         * The MIME content type of the document.
         */
        content_type?: string;
        /**
         * The original filename of the document.
         */
        filename?: string;
        /**
         * The size of the document in bytes.
         */
        size?: number;
    }
}
export interface DocumentCreateParams {
    /**
     * The unique identifier for the associated object.
     */
    documentable_id: string;
    documentable_type: 'counterparties' | 'expected_payments' | 'external_accounts' | 'identifications' | 'incoming_payment_details' | 'internal_accounts' | 'organizations' | 'payment_orders' | 'transactions' | 'connections';
    file: Uploadable;
    /**
     * A category given to the document, can be `null`.
     */
    document_type?: string;
}
export interface DocumentListParams extends PageParams {
    /**
     * The unique identifier for the associated object.
     */
    documentable_id?: string;
    /**
     * The type of the associated object. Currently can be one of `payment_order`,
     * `transaction`, `expected_payment`, `counterparty`, `organization`, `case`,
     * `internal_account`, `decision`, or `external_account`.
     */
    documentable_type?: 'counterparties' | 'expected_payments' | 'external_accounts' | 'identifications' | 'incoming_payment_details' | 'internal_accounts' | 'organizations' | 'payment_orders' | 'transactions' | 'connections';
}
export declare namespace Documents {
    export { type Document as Document, type DocumentsPage as DocumentsPage, type DocumentCreateParams as DocumentCreateParams, type DocumentListParams as DocumentListParams, };
}
//# sourceMappingURL=documents.d.ts.map
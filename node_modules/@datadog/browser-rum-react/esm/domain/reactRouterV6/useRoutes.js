import { useRef } from 'react';
import { matchRoutes, useLocation, useRoutes as originalUseRoutes } from 'react-router-dom';
import { startReactRouterView } from './startReactRouterView';
export var useRoutes = function (routes, locationArg) {
    var location = useLocation();
    var pathname = typeof locationArg === 'string' ? locationArg : (locationArg === null || locationArg === void 0 ? void 0 : locationArg.pathname) || location.pathname;
    var pathnameRef = useRef(null);
    if (pathnameRef.current !== pathname) {
        pathnameRef.current = pathname;
        var matchedRoutes = matchRoutes(routes, pathname);
        if (matchedRoutes) {
            startReactRouterView(matchedRoutes);
        }
    }
    return originalUseRoutes(routes, locationArg);
};
//# sourceMappingURL=useRoutes.js.map
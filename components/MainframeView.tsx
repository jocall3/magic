import React from 'react';
import Card from '../../Card';

const MainframeView: React.FC = () => {
    return (
        <Card title="Mainframe Gateway">
            <p className="text-gray-400">This view will provide tools for securely integrating with and migrating from legacy mainframe systems.</p>
        </Card>
    );
};

export default MainframeView;

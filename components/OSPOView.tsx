import React from 'react';
import Card from '../../Card';

const OSPOView: React.FC = () => {
    return (
        <Card title="Open Source Program Office">
            <p className="text-gray-400">A dashboard for managing open source contributions, license compliance, and community engagement.</p>
        </Card>
    );
};

export default OSPOView;

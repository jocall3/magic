import React from 'react';
import Card from '../../Card';

const DataMeshView: React.FC = () => {
    return (
        <Card title="Data Mesh">
            <p className="text-gray-400">This view will manage decentralized, domain-oriented data products across the enterprise.</p>
        </Card>
    );
};

export default DataMeshView;

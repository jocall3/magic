import React from 'react';
import Card from '../../Card';

const AIRiskRegistryView: React.FC = () => {
    return (
        <Card title="AI Risk Registry">
            <p className="text-gray-400">A centralized log for tracking and mitigating risks associated with the use of AI models across the platform.</p>
        </Card>
    );
};

export default AIRiskRegistryView;
